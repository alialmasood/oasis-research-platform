# 🚀 كيفية تشغيل المشروع

## تشغيل الخادم

افتح Terminal/PowerShell في مجلد المشروع وقم بتشغيل:

```bash
npm run dev
```

لإيقاف الخادم، اضغط:
- **Ctrl + C** (مرة واحدة أو مرتين)

## أوامر مفيدة

### إيقاف جميع عمليات Node.js (إذا لم تستجب Ctrl+C)

**في PowerShell:**
```powershell
Get-Process | Where-Object {$_.ProcessName -like "*node*"} | Stop-Process -Force
```

**في Command Prompt:**
```cmd
taskkill /F /IM node.exe
```

### إعادة تشغيل الخادم

1. أوقف الخادم بـ Ctrl+C
2. شغله مرة أخرى: `npm run dev`

## ملاحظات

- الخادم يعمل على: http://localhost:3000
- لا تقم بإغلاق Terminal أثناء تشغيل الخادم
- إذا واجهت مشاكل، أوقف الخادم وأعد تشغيله
