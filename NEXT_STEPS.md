# 📋 الخطوات التالية لإكمال إعداد المشروع

## ✅ ما تم إنجازه حتى الآن

1. ✅ إعداد المشروع الأساسي (Next.js + TypeScript)
2. ✅ إعداد Tailwind CSS + shadcn/ui + RTL
3. ✅ إعداد Prisma Schema مع نظام الأدوار والصلاحيات
4. ✅ إنشاء docker-compose.yml
5. ✅ إعداد دوال قاعدة البيانات (verify_login, hash_password)
6. ✅ تحديث نظام المصادقة
7. ✅ إنشاء الصفحات الأساسية (login, admin dashboard, researcher dashboard)

## 🔄 الخطوات المتبقية

### 1️⃣ إنشاء ملف .env

```bash
# انسخ ملف .env.example إلى .env
cp .env.example .env
```

أو أنشئ ملف `.env` يدوياً وأضف:

```env
DATABASE_URL="postgresql://research_app:BASbas@2026@localhost:5443/research_platform?schema=public"
DB_HOST=localhost
DB_PORT=5443
DB_NAME=research_platform
DB_USER=research_app
DB_PASSWORD=BASbas@2026
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"
NODE_ENV="development"
```

**مهم**: غيّر `NEXTAUTH_SECRET` إلى قيمة عشوائية آمنة.

### 2️⃣ إعداد صلاحيات قاعدة البيانات

```bash
npm run db:setup-permissions
```

### 3️⃣ إنشاء الجداول في قاعدة البيانات

```bash
# توليد Prisma Client
npm run db:generate

# إنشاء الجداول
npm run db:push
```

### 4️⃣ إضافة البيانات التجريبية

```bash
npm run db:seed
```

سيتم إنشاء:
- مستخدم مدير: `admin@uobasrah.edu.iq` / `admin123`
- مستخدم باحث: `researcher@uobasrah.edu.iq` / `researcher123`
- أقسام، أدوار، صلاحيات
- بيانات تجريبية للأنشطة العلمية

### 5️⃣ تشغيل المشروع

```bash
npm run dev
```

افتح المتصفح على: http://localhost:3000

## 🧪 اختبار النظام

1. **تسجيل الدخول كمدير**:
   - البريد: `admin@uobasrah.edu.iq` أو اسم المستخدم: `admin`
   - كلمة المرور: `admin123`
   - يجب أن يوجهك إلى `/admin/dashboard`

2. **تسجيل الدخول كباحث**:
   - البريد: `researcher@uobasrah.edu.iq` أو اسم المستخدم: `researcher`
   - كلمة المرور: `researcher123`
   - يجب أن يوجهك إلى `/researcher/dashboard`

## 📝 ملاحظات مهمة

- تأكد من أن Docker container يعمل قبل تشغيل أي أوامر قاعدة البيانات
- إذا واجهت أخطاء في Prisma، تأكد من أن `DATABASE_URL` صحيح في ملف `.env`
- تأكد من تشغيل `db:setup-functions` قبل `db:seed` (تم بالفعل ✅)

## 🚀 بعد إكمال الإعداد

بعد إكمال جميع الخطوات، يمكنك:
1. استكشاف الواجهات المختلفة
2. إضافة المزيد من الصفحات والوظائف
3. تخصيص التصميم حسب الحاجة
4. إضافة المزيد من الوحدات العلمية
